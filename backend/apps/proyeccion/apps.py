from django.apps import AppConfig


class ProyeccionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.proyeccion'
    verbose_name = 'Proyección (Budget Estimation)'
